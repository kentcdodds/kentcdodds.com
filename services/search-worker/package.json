{
  "name": "search-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "lint": "oxlint . --ignore-pattern worker-configuration.d.ts --ignore-pattern .wrangler",
    "test": "vitest --run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --env local --port 8787",
    "start": "wrangler dev --env local --port 8787",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@kcd-internal/search-shared": "file:../search-shared",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260306.1",
    "@epic-web/config": "^2.0.0",
    "@types/node": "^25.5.0",
    "oxlint": "^1.51.0",
    "prettier": "^3.8.1",
    "typescript": "^6.0.2",
    "vitest": "^4.0.18",
    "wrangler": "^4.75.0"
  },
  "prettier": "@epic-web/config/prettier"
}
