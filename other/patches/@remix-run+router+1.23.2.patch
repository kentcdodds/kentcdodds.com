diff --git a/node_modules/@remix-run/router/dist/router.js b/node_modules/@remix-run/router/dist/router.js
index af07007..86a97b4 100644
--- a/node_modules/@remix-run/router/dist/router.js
+++ b/node_modules/@remix-run/router/dist/router.js
@@ -3806,6 +3806,7 @@ function normalizeNavigateOptions(normalizeFormMethod, isFetcher, path, opts) {
           formAction,
           formEncType: opts.formEncType,
           formData: undefined,
+          headers: opts.headers,
           json: undefined,
           text
         }
@@ -3824,6 +3825,7 @@ function normalizeNavigateOptions(normalizeFormMethod, isFetcher, path, opts) {
             formAction,
             formEncType: opts.formEncType,
             formData: undefined,
+            headers: opts.headers,
             json,
             text: undefined
           }
@@ -3861,6 +3863,7 @@ function normalizeNavigateOptions(normalizeFormMethod, isFetcher, path, opts) {
     formAction,
     formEncType: opts && opts.formEncType || "application/x-www-form-urlencoded",
     formData,
+    headers: opts && opts.headers,
     json: undefined,
     text: undefined
   };
@@ -4477,6 +4480,7 @@ function createClientSideRequest(history, location, signal, submission) {
     // won't be properly normalized to uppercase and results in a 405 error.
     // See: https://fetch.spec.whatwg.org/#concept-method
     init.method = formMethod.toUpperCase();
+    init.headers = submission.headers ? new Headers(submission.headers) : undefined;
     if (formEncType === "application/json") {
       init.headers = new Headers({
         "Content-Type": formEncType
